;; (use-package lispy
;;   :ensure t
;;   :config
;;   (remove-hook 'emacs-lisp-mode-hook (lambda () (lispy-mode 1)))
;;   (remove-hook 'clojure-mode-hook (lambda () (lispy-mode 1))))

(provide 'lang-elisp)
